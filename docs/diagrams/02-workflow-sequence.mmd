sequenceDiagram
  autonumber
  participant U as User
  participant UI as Web UI
  participant LS as LocalStorage(Mock)

  U->>UI: Buka Landing (Index)
  UI->>LS: Load seed data (jika belum ada)
  LS-->>UI: users + songs + collab_requests
  UI-->>U: Tampilkan daftar request OPEN

  U->>UI: Register (nama, email, password, role)
  UI->>LS: Create user
  LS-->>UI: userId
  UI-->>U: Arahkan ke Login

  U->>UI: Login (email, password)
  UI->>LS: Validate credentials
  LS-->>UI: session (userId, role)
  UI-->>U: Redirect ke Dashboard

  U->>UI: Upload Lagu Demo
  UI->>LS: Create song metadata
  LS-->>UI: songId
  UI-->>U: Success toast + balik ke Dashboard
